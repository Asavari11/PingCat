# Root environment for Vite (frontend) and Electron

# Base URL for API requests from the React app
VITE_API_URL=http://localhost:5000

# Gemini API key used by Electron AI integration
# Ensure electron/main.ts reads this from process.env in your environment
GEMINI_API_KEY=your_gemini_key_here

# Optional: Electron development URL (usually set via npm script)
# ELECTRON_START_URL=http://localhost:8080
